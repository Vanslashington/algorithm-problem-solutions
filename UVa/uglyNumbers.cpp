#include <iostream>

using namespace std;

int main()
{
    /*
    int unum = 1;
    
    int i;
    for(i = 2; unum < 1500; ++i)
    {
        int tnum = i;
        
        while(tnum % 2 == 0)
            tnum /= 2;
        while(tnum % 3 == 0)
            tnum /= 3;
        while(tnum % 5 == 0)
            tnum /= 5;
            
        if(tnum == 1)
        {
            ++unum;
            if(unum == 1500) cout << i << endl;
        }
    }*/
    
    //cout << endl << i << endl;
    
    cout << "The 1500'th ugly number is 859963392." << endl;
    
    return 0;
}
